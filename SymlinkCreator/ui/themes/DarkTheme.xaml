<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ========== ULTRA DARK THEME ========== -->
  <Color x:Key="AppBackgroundColor">#0A0A0E</Color>
  <Color x:Key="AppSurfaceColor">#0F0F13</Color>
  <Color x:Key="AppTextColor">#FFFFFF</Color>
  <Color x:Key="AppBorderColor">#2A2A35</Color>
  <Color x:Key="AppAccentColor">#89B4FA</Color>

  <!-- ========== BRUSHES ========== -->
  <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0A0A0E" />
  <SolidColorBrush x:Key="AppSurfaceBrush" Color="#0F0F13" />
  <SolidColorBrush x:Key="AppBorderBrush" Color="#2A2A35" />
  <SolidColorBrush x:Key="AppTextBrush" Color="#FFFFFF" />
  <SolidColorBrush x:Key="AppSubtextBrush" Color="#888888" />
  <SolidColorBrush x:Key="AppMutedBrush" Color="#A0A0A0" />
  <SolidColorBrush x:Key="AppAccentBrush" Color="#89B4FA" />
  <SolidColorBrush x:Key="AppAccentHoverBrush" Color="#B4D0FF" />
  <SolidColorBrush x:Key="AppOverlayBrush" Color="#1A1A20" />
  <SolidColorBrush x:Key="AppSuccessBrush" Color="#A6E3A1" />
  <SolidColorBrush x:Key="AppErrorBrush" Color="#F38BA8" />
  <SolidColorBrush x:Key="AppYellowBrush" Color="#F9E2AF" />
  <SolidColorBrush x:Key="AppAccentSemiTransparentBrush" Color="#89B4FA" Opacity="0.3" />
  <SolidColorBrush x:Key="AppOverlaySemiTransparentBrush" Color="#1A1A20" Opacity="0.5" />

  <!-- ========== WINDOW STYLE ========== -->
  <Style TargetType="{x:Type Window}">
    <Setter Property="Background" Value="{StaticResource AppBackgroundBrush}" />
    <Setter Property="Foreground" Value="{StaticResource AppTextBrush}" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="FontSize" Value="12" />
  </Style>

  <!-- ========== BUTTON STYLE ========== -->
  <Style TargetType="{x:Type Button}">
    <Setter Property="Background" Value="#1A1A20" />
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="BorderBrush" Value="#2A2A35" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="12,6" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Padding="{TemplateBinding Padding}"
                  CornerRadius="2">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#262630" />
      </Trigger>
      <Trigger Property="IsPressed" Value="True">
        <Setter Property="Background" Value="#303039" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- ========== TEXTBOX STYLE ========== -->
  <Style TargetType="{x:Type TextBox}">
    <Setter Property="Background" Value="#1A1A20" />
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="BorderBrush" Value="#2A2A35" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="8,6" />
    <Setter Property="FontFamily" Value="Segoe UI" />
    <Setter Property="CaretBrush" Value="#89B4FA" />
  </Style>

  <!-- ========== LABEL STYLE ========== -->
  <Style TargetType="{x:Type Label}">
    <Setter Property="Foreground" Value="#E0E0E0" />
    <Setter Property="FontFamily" Value="Segoe UI" />
  </Style>

  <!-- ========== TEXTBLOCK STYLE ========== -->
  <Style TargetType="{x:Type TextBlock}">
    <Setter Property="FontFamily" Value="Segoe UI" />
  </Style>

  <!-- ========== LISTVIEW STYLE ========== -->
  <Style TargetType="{x:Type ListView}">
    <Setter Property="Background" Value="#0A0A0E" />
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="BorderBrush" Value="#2A2A35" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style TargetType="{x:Type ListViewItem}">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="Padding" Value="8,4" />
  </Style>

  <!-- ========== CHECKBOX STYLE ========== -->
  <Style TargetType="{x:Type CheckBox}">
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="FontFamily" Value="Segoe UI" />
  </Style>

  <!-- ========== BORDER STYLE ========== -->
  <Style TargetType="{x:Type Border}">
    <Setter Property="Background" Value="#0A0A0E" />
  </Style>

  <!-- ========== GRID STYLE ========== -->
  <Style TargetType="{x:Type Grid}">
    <Setter Property="Background" Value="#0A0A0E" />
  </Style>

  <!-- ========== STACKPANEL STYLE ========== -->
  <Style TargetType="{x:Type StackPanel}">
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <!-- ========== DOCKPANEL STYLE ========== -->
  <Style TargetType="{x:Type DockPanel}">
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <!-- ========== SCROLLVIEWER STYLE ========== -->
  <Style TargetType="{x:Type ScrollViewer}">
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <!-- ========== SCROLLBAR STYLE ========== -->
  <Style TargetType="{x:Type ScrollBar}">
    <Setter Property="Background" Value="#0A0A0E" />
    <Setter Property="Foreground" Value="#2A2A35" />
  </Style>

  <!-- ========== SEPARATOR STYLE ========== -->
  <Style TargetType="{x:Type Separator}">
    <Setter Property="Background" Value="#2A2A35" />
    <Setter Property="Height" Value="1" />
    <Setter Property="Margin" Value="0,4" />
  </Style>

  <!-- ========== COMBOBOX STYLE ========== -->
  <Style TargetType="{x:Type ComboBox}">
    <Setter Property="Background" Value="#1A1A20" />
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="BorderBrush" Value="#2A2A35" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="8,6" />
    <Setter Property="FontFamily" Value="Segoe UI" />
  </Style>

  <!-- ========== RADIOBUTTON STYLE ========== -->
  <Style TargetType="{x:Type RadioButton}">
    <Setter Property="Foreground" Value="#FFFFFF" />
    <Setter Property="FontFamily" Value="Segoe UI" />
  </Style>

  <!-- ========== GROUPBOX STYLE ========== -->
  <Style TargetType="{x:Type GroupBox}">
    <Setter Property="Foreground" Value="#E0E0E0" />
    <Setter Property="BorderBrush" Value="#2A2A35" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="8,8" />
    <Setter Property="Background" Value="#0A0A0E" />
  </Style>

</ResourceDictionary>
